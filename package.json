{
  "name": "astrofusion-design-system",
  "version": "0.0.1",
  "private": true,
  "description": "The LLM-First Design System. Built for Humans & Agents.",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "setup": "node scripts/rename-project.js"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@testing-library/react": "^14.0.0",
    "jsdom": "^23.0.0"
  },

  "packageManager": "pnpm@10.26.2"
}
